class CSVUtil{
    // In class we could not write function like ---> / const func = () +> {}  /
    public csvToArray = (text: string): string[] => {
        return ['',''];
    };
    public readCsvFile(){
        return 'OK';
    };

}

export const csvUtil = new CSVUtil();